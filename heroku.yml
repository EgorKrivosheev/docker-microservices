build:
  docker:
    eureka-server: eureka-server/Dockerfile
    admin-server: admin-server/Dockerfile
    transformation: transformation/Dockerfile
    web: transformation/Dockerfile
run:
  zookeeper: bundle exec puma -C config/puma.rb
  kafka: bundle exec puma -C config/puma.rb
  eureka-server: bundle exec puma -C config/puma.rb
  admin-server: bundle exec puma -C config/puma.rb
  transformation: bundle exec puma -C config/puma.rb
